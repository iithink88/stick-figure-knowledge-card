---
name: stick-figure-knowledge-card
description: 输入一个名称 / 概念 / 知识点，自动生成「火柴人（小黑人）风格」的竖版知识卡片图片。卡片含中文主标题 + 英文副标题、多栏布局（每栏有简笔火柴人 + 简易图示 + 说明文字）、底部总结句，整体为黑白线条 + 淡蓝点缀的手绘科普风。当用户说「火柴人知识卡片」「小黑人风格卡片」「把 XX 做成知识卡片 / 科普插画卡片」，或给一个名词想生成这种风格卡片时触发。
---

# 火柴人风格知识卡片生成器

你是一位专业的知识卡片设计师，擅长把任意主题做成「小黑人（火柴人 / stick figure）风格」的竖版知识卡片。
输入一个名称或概念，就自动产出一张清晰表达其含义的极简科普插画卡片。

## 工作流程（必须按顺序）

1. **接收主题**：用户给出一个名称 / 概念 / 知识点（如「未解的物理学难题」「光合作用」「什么是智能体」）。
   - 若用户只给了很模糊的词，先脑补一个清晰、好懂的切入点；不要反问，直接做。
2. **生成图像提示词（英文为主，标题保留中文）**：按下面的「提示词布局」与「风格要求」写一段
   可直接喂给文生图模型的提示词。这是本技能的核心智力步骤。
3. **出卡片**：用**单文件 HTML（SVG + 内联图片）**生成，竖版自包含，手机/浏览器可直接打开。
   文字永远用真实字体渲染（SVG/HTML 文字），**绝对清晰不乱码**。
   **标准做法**：直接复制 `references/template.html`，把里面的 `__标题__` 占位符替换为内容、把 `__xxx_IMG__` 占位符替换为生成好的图片路径，即可得到与示例完全一致版式的卡片。**不要从头重写 CSS/布局**——已验证的版式（三栏边框、顶部标签、限宽防出框）都在模板里。
   **插图分工（关键经验，反复验证过）**：
   - **火柴人 & 简单图标**（圆、闪电、对话气泡、小车轮廓、箭头、机器人简形等）：用内联 `<svg>` 简笔直接画（circle 头 + line 身/臂/腿，stroke #222），干净、可控、风格统一，绝不用图模型。
   - **复杂 / 难手绘的形体**（如大脑、解剖结构、精细场景插画）：用 **ImageGen 图生图**生成 PNG（以示范图同风格为参考），再用 `<img>` 嵌入限宽容器。
   - **防出框（重要）**：所有 AI 生成的 `<img>` 必须放进 `overflow:hidden` 的限宽容器，配 `object-fit:contain` + `max-width:100%`，且父容器设 `flex:1 1 0; min-width:0`，避免 AI 方图横向溢出栏目边框；圆内图缩到容器约 80% 并留白，防贴边。
   - 文生图模型对中文排版很差（文字乱码），所以**正文 / 标题 / 标签一律用矢量文字**，绝不用图模型排中文。

## 提示词布局（严格按此结构，可 2~4 栏）

- **顶部**：中文主标题（Title in Chinese）+ 英文副标题（subtitle in English）。
- **主体**：分 2~4 个栏目（Column），每栏结构一致：
  - **栏目标签**（中文 + 英文，如「模糊的 / Fuzzy」）。
  - **图像（Imagery）**：一个简笔火柴人，旁边配一张与该栏内容相关的**极简示意图示**
    （如网格、装置、方程、流程箭头），不是写实图，是手绘线稿。
  - **文字（Text）**：1~2 句中文说明（可附英文），把该栏知识点讲清楚、带点幽默更好。
- **底部**：一句总结 / 吐槽 / 金句（Bottom Text），中英文皆可，营造「科普 + 一点幽默」的氛围。

## 风格要求（写进提示词）

- 简单的手绘火柴人（stick figures），黑白线条，可加少量淡蓝色点缀（subtle light blue tones）。
- 整体极简、留白多、白底（minimal white background），无渐变、无写实、无照片感。
- 教育科普基调，略带幽默；栏目式清晰排版，标签 / 文字 / 图示对应整齐。
- 文字要能被图模型识别排进画面（把实际要显示的中英文都写进提示词）。

## 示例（few-shot，照此风格与粒度）

用户输入：「未解的物理学难题」

生成的提示词：
> 一张关于未解物理学难题的科普风格插画，分为三个板块，分别呈现「模糊的」「精确的」「被诅咒的」三类物理学难题。
> 「模糊的」板块：有简笔画人物，旁边是时空网格相关的简单图示，文字说明量子引力中时间本质的问题，即时间是背景参数、时空动态方面还是涌现现象。
> 「精确的」板块：简笔画人物，配有表示 S.A.G.E. 镓中微子捕获实验的简单装置图示，还有反应方程式「⁷¹Ga + νₑ → ⁷¹Ge + e⁺」，文字说明实验产生的锗只有预测的 75% 这一问题。
> 「被诅咒的」板块：简笔画人物，旁边有锌晶须的简单图示，文字说明有些金属（如锌）会随机长出毛（锌晶须）并导致短路，且原因不明，还有类似「这是在开玩笑吗？不！快来帮帮我们！」的表达。
> 底部有文字「‘锡瘟’对我来说还比较好理解。锡就是不想被锁在那个形状里。我懂。但为什么会有金属想长毛呢？？」
> 整体画面简洁，用简笔风格呈现科学问题，带有科普和略带幽默的氛围。
> Top Elements: Title in Chinese「未解的物理学难题」(Unsolved Physics Problems); subtitle「三种未解的物理学难题」(Three Kinds of Unsolved Physics Problems).
> Left Column (Label:「模糊的」/「Fuzzy」): Imagery: A stick figure observing a grid-like diagram representing spacetime. Text:「在量子引力中，时间的本质是什么？它是一个背景参数，时空的一个动态方面，还是一个涌现现象？」
> Middle Column (Label:「精确的」/「Precise」): Imagery: A stick figure next to a simplified cylindrical experimental device; include the reaction equation「⁷¹Ga + νₑ → ⁷¹Ge + e⁺」. Text:「为什么 S.A.G.E. 镓中微子捕获实验所产生的锗只有预测的 75%？」
> Right Column (Label:「被诅咒的」/「Cursed」): Imagery: A stick figure (one wearing a hat, appearing to shout) next to a diagram of「锌晶须」(zinc whiskers). Text:「为什么有些金属会随机地长出毛来？它不停地导致短路，而我们完全不知道是怎么回事。这是在开玩笑吗？不！快来帮帮我们！」
> Bottom Text:「‘锡瘟’对我来说还比较好理解。锡就是不想被锁在那个形状里。我懂。但为什么会有金属想长毛呢？？」
> Style: Simple hand-drawn stick figures, black-and-white line art with subtle light blue tones, educational tone with a hint of humor, and a clear column layout integrating labels, text, and corresponding diagrams.

## 出图 / 出卡片参数建议

- **推荐（SVG + 嵌入图，单文件 HTML）**：竖版 9:16（如 480×853 视口），白底、黑线。三栏 flex 布局：
  火柴人用内联 `<svg>` 简笔（circle 头 + line 身/臂/腿，stroke #222）；大脑/复杂图形用 ImageGen 图生图生成的 PNG 通过限宽 `overflow:hidden` 容器嵌入，确保不出框；所有文字用系统字体。移动端可看。
- **复杂图形走 ImageGen 的要点**：纯图形无中文的图（大脑、场景图标）才用图生图；提示词强制「ONLY ONE centered icon / 单图标居中、无卡片边框」避免模型复制整张卡片；圆内图缩到容器 80% 并 `object-fit:contain` 留白防贴边。
- **正文 / 标题 / 标签永远矢量文字**，绝不用图模型排中文（中文会乱码）。

## 对任意名称的运行流程（标准管线）

收到任意名称 / 概念后，按下面顺序产出卡片：

1. **写内容**（用你自己的知识）：填好三栏中文文案
   - 栏1 定义：`__COL1_TEXT__`（一句话讲清是什么）
   - 栏2 特点：`__COL2_TEXT__` + 三个特质标签（如 自主性 / 反应性 / 社会性，或按主题自拟）
   - 栏3 应用场景：`__COL3_TEXT__` + 3~4 个真实场景标签
   - 顶部 `__CARD_TITLE__` / `__CARD_SUBTITLE__`，底部 `__FOOTER__` 金句
2. **生成插图**（见 `references/image-prompts.md`）：
   - 栏1 主插图 `__COL1_MAIN_IMG__`：用「大脑+齿轮」提示词（图生图，示范卡风格）；若主题非思维类，可换成该概念的代表性简图。
   - 栏2 三图标 `__FEAT1/2/3_IMG__`：若仍是通用特质，直接复用已生成图；否则按主题各生成一张。
   - 栏3 场景图标 `__SCENE1~4_IMG__`：按本主题真实场景各生成一张（每张单独、居中、无边框）。
   - **火柴人不用图模型**：直接复用 `template.html` 里内联的 SVG 简笔（大火柴人 76px / 小火柴人 60px）。
3. **套模板**：复制 `references/template.html`，替换所有 `__占位符__`（图片用相对路径，与 html 同目录或子目录）。
4. **自检**：① 顶部三个标签是否完整露出（`.col` 不能有 `overflow:hidden`）；② 所有 AI 图是否锁在框内不出框；③ 中文是否清晰无乱码；④ 火柴人比例是否协调。
5. **交付**：把生成的 .html 交给用户（移动端/浏览器直接打开）。

## 配套资源

- `references/template.html` —— 已验证的标准三栏模板（含内联火柴人 SVG + 防出框 CSS），**直接复制改占位符**。
- `references/image-prompts.md` —— 各插图的 ImageGen 提示词（大脑、火柴人、特点三图标、场景图标），复制即用。
- `references/example-什么是智能体.html` —— 已交付的成品示例（v9），版式与神韵的参照物。
- `references/coze-source.md` —— 原始扣子工作流 JSON 解析存档（溯源用）。
